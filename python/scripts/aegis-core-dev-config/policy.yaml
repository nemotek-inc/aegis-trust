# Dev fixture: minimal threat + audit policy.
# Mirrors the gateway integration-test configuration used upstream, so β
# integration tests see the same gateway shape production does.
threat:
  max_open_capsules: 3
audit:
  chain_verify: true
