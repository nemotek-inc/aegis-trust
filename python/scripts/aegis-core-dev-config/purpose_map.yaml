# Dev fixture: empty purpose map, loaded by aegis-gateway on startup.
# The gateway requires this file at startup; the same shape is used by the
# gateway's own integration-test configuration upstream.
# Real deployments populate this from their purpose catalog.
purposes: {}
