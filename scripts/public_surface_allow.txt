# public_surface_guard の宣言された例外。
#
# 形式:  <path> | <規則> | <理由>
#
# **理由の空欄は宣言ではない。** 理由を書かずに増やせる例外は統制ではないので、
# 門は理由の無い行を読み込みで落とす。
#
# 1 行足す前に考えること: その file が「公開してよいもの」なのか、それとも
# **門の側が正しくて中身を直すべき**なのか。2026-09-17 の初回走査では 9 件
# 当たり、うち 3 件は実際に private 側の path を公開 repo に書いていた
# (中身を直した)。例外にしたのは、規則では表現できない 3 件だけ。

# 存在しない path を「存在しないこと」の対照として使っている試験。
# この 2 本は登録簿に載っていない runner を弾けるかを測る陰性対照なので、
# **実在しない path が在ることが仕様**。
node/tests/conformanceRunnerHygiene.test.ts | R1 | 未登録 runner の陰性対照。実在しない path を意図的に書いている
python/tests/test_conformance_runner_hygiene.py | R1 | 未登録 runner の陰性対照。実在しない path を意図的に書いている

# 変更履歴は「その時そこに在った」を記録するもので、現在の tree と一致しない
# のが正常。過去の記述を現在の tree に合わせて書き換えると履歴が嘘になる。
python/CHANGELOG.md | R1 | 変更履歴。当時存在した path を記録しており、現在の tree と一致しないのが正常

# 門そのものの対照。実在しない path を書くことが仕事の file なので、R1 が当たるのは
# 正しい。ここを例外にしないと、門を試験できる file が 1 つも書けなくなる。
python/tests/test_public_surface_guard.py | R1 | 門の陽性対照。実在しない path を意図的に書いている
