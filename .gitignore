# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
dist/
build/
.venv/
venv/
.ci-venv/
.ci-venv-audit/
.release-venv/

# Node
node_modules/
*.tgz
coverage/
.vite/

# Root-level npm install sandbox (smoke-tests published rc tarballs against the
# canonical `node/package.json`). The repo root is not an npm package itself;
# `node/` is. These appear when contributors run `npm install aegis-trust` at
# the root for ad-hoc verification — keep them local-only.
/package.json
/package-lock.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Aegis runtime
aegis-sandbox-audit.jsonl
*.jsonl.tmp

# Secrets (defense in depth)
.env
.env.local
.pypirc
.npmrc

# Internal sprint / audit artifacts — never publish to the public repo.
# These hold ops-internal review evidence (exposure audits, cross-review logs)
# that reference private substrate paths. Kept local-only.
#
# **Anchored to the repo root (S053).** Without the leading slash this pattern
# matches a `deploy/` at ANY depth, so a customer-facing deployment unit written
# to `docs/deploy/` disappeared silently — the file was created, `git add`
# refused it, and nothing said why unless someone looked. The intent was always
# this one top-level scratch directory.
/deploy/

# 道具が repo の中に書く作業 file。**この repo は公開されている。**
#
# 2026-09-17 実測: browser 道具の session log 4 本 (console / network / audit) が
# 未追跡のまま作業ツリーに在った。中身に private 側の語は無かったが、network log は
# 素性が読めない — そして **未追跡の file は `git add -A` 1 回で公開される**。
#
# 「公開 repo の作業ツリーを道具の置き場に使わない」を、人の注意ではなく
# ここで決める (2026-09-16 の起票 #285 の 3 番目の項目)。
/.gstack/
